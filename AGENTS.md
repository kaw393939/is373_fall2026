<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->


# IS373 project context

Read `README.md`, `docs/project-overview.md`, and `docs/project-map.md` before working. Read `docs/my-discovery-brief.md` if the student has created it; do not invent their answers.

The current phase is student discovery and the existing portfolio starter. AI assists development; it is not an in-app feature. Docker and containerization are part of the learning objectives. Supplemental course material belongs in `docs/` as Markdown linked from the README. Preserve the existing app and learning pages unless the user requests changes.

Do not implement a CMS, blog engine, QR integration, authentication, analytics, or external AI service based only on the roadmap. Future capabilities in `docs/project-vision.md` require a new request. Keep local content files for this phase.

Preserve unrelated work. Keep sample projects clearly identified; never fabricate professional or research evidence. Use semantic theme tokens and preserve keyboard access and the neutral Base theme. Read installed Next.js guides before framework edits, as required above.

Use `docs/development.md` and `docs/portfolio-starter.md` for verification commands. For documentation changes, verify local links and command accuracy. For application changes, run appropriate lint/type/build and affected browser checks. Report only checks actually performed. Update the project map and student handoff when relevant behavior changes.
